# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0

# Optional: Native WebRTC support (fallback when go2rtc is not available).
# Not installed by default - install with:
#   pip install ipycam[webrtc]
# or manually:
#   pip install aiortc>=1.14.0 aiohttp>=3.9.0

# Optional: for better performance
# opencv-contrib-python>=4.8.0
